spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///forge
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true
  data:
    redis:
      host: localhost
      port: 16381
